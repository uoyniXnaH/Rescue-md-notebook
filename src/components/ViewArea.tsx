import { Box } from "@mui/material";

function ViewArea() {
  return (
    <Box width="41%" maxWidth={788} sx={{ bgcolor: "primary.main", color: "primary.contrastText" }}>
      <h1>Rescue Notebook</h1>
    </Box>
  );
}

export default ViewArea;